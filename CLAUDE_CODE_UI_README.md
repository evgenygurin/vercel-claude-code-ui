# Claude Code UI - AI-Powered Development Environment

Современный веб-интерфейс для управления сессиями и проектами Claude Code с интеграцией Vercel v0 Platform API.

## 🚀 Возможности

### Управление сессиями Claude
- ✅ Создание и управление сессиями AI
- ✅ Настройка параметров модели (температура, max tokens)
- ✅ История сообщений и контекст
- ✅ Статусы сессий (активная, неактивная, архивированная)

### Управление проектами
- ✅ Организация сессий по проектам
- ✅ Настройки проекта по умолчанию
- ✅ Управление файлами проекта
- ✅ Статистика и метрики

### Интеграция с v0 Platform API
- ✅ Генерация React компонентов
- ✅ Автоматическое развертывание на Vercel
- ✅ Интеграция с существующим кодом
- ✅ Поддержка Tailwind CSS

### Инструменты разработки
- ✅ **fd** - Быстрый поиск файлов
- ✅ **ripgrep** - Продвинутый поиск по коду
- ✅ **ast-grep** - Анализ структуры кода
- ✅ **jq** - Обработка JSON данных
- ✅ **yq** - Управление YAML файлами

## 🛠 Технологический стек

- **Frontend**: Next.js 15, React 18, TypeScript
- **UI**: Tailwind CSS, Radix UI, Lucide Icons
- **Backend**: Next.js API Routes
- **Хранение**: LocalStorage (можно расширить до базы данных)
- **Интеграции**: Vercel v0 Platform API, Claude API

## 📦 Установка

1. **Клонируйте репозиторий:**
```bash
git clone <your-repo-url>
cd vercel-claude-code-ui
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Настройте переменные окружения:**
Создайте файл `.env.local` в корне проекта:
```env
# Vercel v0 Platform API Configuration
V0_API_KEY=your_v0_api_key_here

# Claude API Configuration (опционально)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Next.js Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

4. **Запустите приложение:**
```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

## 🔑 Получение API ключей

### Vercel v0 Platform API
1. Зарегистрируйтесь на [v0.dev](https://v0.dev)
2. Перейдите в настройки аккаунта
3. Создайте API ключ
4. Добавьте ключ в `.env.local` как `V0_API_KEY`

### Claude API (опционально)
1. Зарегистрируйтесь на [Anthropic](https://console.anthropic.com/)
2. Создайте API ключ
3. Добавьте ключ в `.env.local` как `ANTHROPIC_API_KEY`

## 🎯 Использование

### Создание сессии Claude
1. Перейдите в раздел "Claude Sessions"
2. Нажмите "New Session"
3. Заполните название и описание
4. Выберите проект (опционально)
5. Настройте параметры модели

### Чат с Claude
1. Выберите сессию из списка
2. Откроется интерфейс чата
3. Задавайте вопросы о разработке
4. Используйте команды для работы с инструментами

### Генерация компонентов с v0
1. В чате введите запрос на генерацию
2. Нажмите кнопку "v0 Generate" (⚡)
3. Получите готовый React компонент
4. Скопируйте код или разверните на Vercel

### Управление проектами
1. Перейдите в раздел "Projects"
2. Создайте новый проект
3. Настройте параметры по умолчанию
4. Добавляйте сессии к проекту

## 🔧 API Endpoints

### Сессии
- `GET /api/sessions` - Получить все сессии
- `POST /api/sessions` - Создать новую сессию
- `GET /api/sessions/[id]` - Получить сессию по ID
- `PUT /api/sessions/[id]` - Обновить сессию
- `DELETE /api/sessions/[id]` - Удалить сессию

### Сообщения
- `GET /api/sessions/[id]/messages` - Получить сообщения сессии
- `POST /api/sessions/[id]/messages` - Добавить сообщение
- `DELETE /api/sessions/[id]/messages` - Очистить сообщения

### Проекты
- `GET /api/projects` - Получить все проекты
- `POST /api/projects` - Создать новый проект
- `GET /api/projects/[id]` - Получить проект по ID
- `PUT /api/projects/[id]` - Обновить проект
- `DELETE /api/projects/[id]` - Удалить проект

### v0 Integration
- `POST /api/v0/generate` - Генерировать компонент
- `POST /api/v0/deploy` - Развернуть на Vercel

## 🎨 Примеры использования

### Поиск файлов с fd
```
Найди все TypeScript файлы в проекте
```

### Поиск кода с ripgrep
```
Найди все React хуки в коде
```

### Анализ структуры с ast-grep
```
Покажи все компоненты React в проекте
```

### Обработка JSON с jq
```
Покажи все зависимости из package.json
```

### Генерация компонентов с v0
```
Создай современную карточку с hover эффектами
```

## 🚀 Развертывание на Vercel

1. **Подключите репозиторий к Vercel:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Импортируйте ваш репозиторий
   - Настройте переменные окружения

2. **Настройте переменные окружения в Vercel:**
   - `V0_API_KEY` - ваш API ключ v0
   - `ANTHROPIC_API_KEY` - ваш API ключ Claude (опционально)

3. **Разверните:**
   - Vercel автоматически развернет приложение
   - Получите URL для доступа

## 🔄 Расширение функционала

### Добавление базы данных
Замените LocalStorage на:
- PostgreSQL с Prisma
- MongoDB с Mongoose
- Supabase
- Firebase

### Интеграция с Git
- Автоматическое создание коммитов
- Синхронизация с GitHub
- Управление ветками

### Дополнительные AI модели
- OpenAI GPT
- Google Gemini
- Local модели через Ollama

## 📝 Структура проекта

```
src/
├── app/                    # Next.js App Router
│   ├── api/               # API routes
│   │   ├── sessions/      # Управление сессиями
│   │   ├── projects/      # Управление проектами
│   │   └── v0/           # v0 интеграция
│   ├── claude/           # Claude интерфейс
│   └── ...
├── components/           # React компоненты
│   ├── claude/          # Claude компоненты
│   ├── ui/              # UI компоненты
│   └── ...
├── lib/                 # Утилиты и сервисы
│   ├── v0-config.ts     # Конфигурация v0
│   ├── v0-service.ts    # Сервис v0
│   └── claude-session-service.ts
└── ...
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License - см. файл LICENSE для деталей.

## 🆘 Поддержка

Если у вас есть вопросы или проблемы:
1. Проверьте документацию
2. Создайте Issue в репозитории
3. Обратитесь к сообществу

---

**Создано с ❤️ для разработчиков, использующих Claude и v0**
